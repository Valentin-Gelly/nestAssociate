info:
  name: get-investments-list
  type: http
  seq: 2

http:
  method: GET
  url: "{{localhost}}/investments"
  headers:
    Authorization: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
