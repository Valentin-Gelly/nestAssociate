info:
  name: get-investments-by-project
  type: http
  seq: 3

http:
  method: GET
  url: "{{localhost}}/investments/{{projectId}}"
  headers:
    Authorization: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
