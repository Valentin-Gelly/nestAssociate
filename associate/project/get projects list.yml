info:
  name: get projects list
  type: http
  seq: 2

http:
  method: GET
  url: "{{localhost}}/api/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
