info:
  name: recommended-projects
  type: http
  seq: 4

http:
  method: GET
  url: "{{localhost}}/projects/recommended"
  headers:
    Authorization: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
