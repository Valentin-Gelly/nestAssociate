info:
  name: get-user-by-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{localhost}}/api/users/{{userId}}"
  headers:
    Authorization: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
