info:
  name: get-user-interests
  type: http
  seq: 3

http:
  method: GET
  url: "{{localhost}}/users/interests"
  headers:
    Authorization: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
