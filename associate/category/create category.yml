info:
  name: create category
  type: http
  seq: 1

http:
  method: POST
  url: "{{localhost}}/api/category"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
